# Raghu Guruvayurappan

A dependency-free personal site ready for GitHub Pages.

## Preview

Open `index.html` in a browser, or serve it locally:

```bash
python3 -m http.server 8000
```

Then visit `http://localhost:8000`.

## Publish

Push the repository to the `main` branch and enable GitHub Pages from that branch in the repository settings.
